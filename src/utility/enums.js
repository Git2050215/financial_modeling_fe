const USER_TYPE = {
  ORDINARY: 0,
  ADMIN: 1,
}

const USER_TYPE_DESC = {
  [USER_TYPE.ORDINARY]: '普通用户',
  [USER_TYPE.ADMIN]: '管理员',
}

const enums = {
  USER_TYPE,
  USER_TYPE_DESC,
}

export default enums